'use client'

// OAuth buttons removed — email/password only for now
// Re-add Google/Apple/Facebook once OAuth apps are configured in Supabase

export function OAuthButtons() {
  return null
}

export function Divider() {
  return null
}
